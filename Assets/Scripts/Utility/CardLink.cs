using UnityEngine;
using System.Collections;

// used to link a prefab instance of a card to the "model" version of the card. No idea if this is a workable pattern
public class CardLink : MonoBehaviour
{
    public Card Card;
}
